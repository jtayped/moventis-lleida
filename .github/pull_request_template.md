<!-- subject line: lowercase, imperative, typed — e.g. fix(web): keep the drawer open across a line toggle -->

## what this changes

<!-- one or two sentences: what behaves differently once this merges? -->

## why

<!-- the problem, not the solution. "fixes #123" if there is an issue. -->

## notes for the reviewer

<!-- trade-offs, things you chose not to do, anything non-obvious. delete if empty. -->

## checks

- [ ] `pnpm lint`, `pnpm typecheck` and `pnpm test` pass locally
- [ ] `pnpm build` passes if the change touches `apps/web`
- [ ] CLAUDE.md / AGENTS.md updated if a command, a package or the data flow changed (see the maintenance note at the top of each)
- [ ] no secret, credential or personal data in the diff

## screenshots

<!-- required for a visual change; light and dark if it touches theming. delete if not applicable. -->
